# Runtime-only dependencies for the deployed Lambdas, pinned for reproducibility.
#
# `infra/deploy.sh` runs `scripts/build_lambda_package.py`, which resolves this
# closure from the project `.venv` and copies it into `.lambda_build`; `sam
# build` then only copies (no pip install, no network). Dev/test-only packages
# (pytest, moto, pypdf) live in `requirements.txt` and are intentionally
# excluded here.

strands-agents==1.53.0
boto3==1.43.83
botocore==1.43.91
python-dotenv==1.2.3
reportlab==4.5.1
google-api-python-client==2.199.0
google-auth-oauthlib==1.4.0
google-auth==2.56.3

# Transitive pins, kept for a fully reproducible runtime set.
httpx==0.28.1
httpx-sse==0.4.3
Jinja2==3.1.6
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
