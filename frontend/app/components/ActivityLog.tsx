import { Action } from "../../lib/api";
export function ActivityLog({ entries }: { entries: Action[] }) { return <section className="ledger-panel activity"><div className="panel-head"><h2>Activity log</h2><span className="count">recent</span></div>{entries.length === 0 ? <div className="empty"><strong>No activity yet</strong>Resolved actions will be recorded here.</div> : entries.map((entry) => <article className="activity-entry" key={entry.action_id}><time>25 AUG 2026 · 09:42</time><p className="small"><strong>{entry.status === "executed" ? "Approved" : entry.status}</strong> — {entry.agent_reasoning}</p></article>)}</section>; }
